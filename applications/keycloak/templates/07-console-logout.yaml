apiVersion: config.openshift.io/v1
kind: Console
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "8"
  name: cluster
spec:
  authentication:
    logoutRedirect: https://sso.{{ .Values.global.hubClusterDomain }}/realms/openshift-oauth/protocol/openid-connect/logout?client_id=idp-4-ocp&post_logout_redirect_uri=https://console-openshift-console.{{ .Values.global.hubClusterDomain }}
