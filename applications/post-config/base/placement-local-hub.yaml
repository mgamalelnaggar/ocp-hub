apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: placement-local-hub
  namespace: open-cluster-management-policies
spec:
  clusterSelector:
    matchLabels:
      local-cluster: "true"
