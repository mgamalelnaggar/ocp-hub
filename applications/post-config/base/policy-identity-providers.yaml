apiVersion: policy.open-cluster-management.io/v1
kind: Policy
metadata:
  name: policy-identity-providers
  namespace: open-cluster-management-policies
spec:
  remediationAction: enforce
  disabled: false
  policy-templates:
    - objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: configure-identity-providers
        spec:
          remediationAction: enforce
          severity: high
          object-templates:
            - complianceType: musthave
              objectDefinition:
                apiVersion: v1
                kind: Secret
                metadata:
                  name: htpasswd-secret
                  namespace: openshift-config
                type: Opaque
                data:
                  htpasswd: b2NwYWRtaW46JDJ5JDA1JFIzUnA0dC41eExGMHJ5U1VtZ0NyYy5zbGRiM2tnVzA5elQyQkQ2N3hKWUY1NVFKVlJaQjhlCg==
            {{ if and (eq (fromConfigMap "default" "ldap-config" "ldap") "enabled") (ne (fromConfigMap "default" "ldap-config" "bindPassword") "") }}
            - complianceType: musthave
              objectDefinition:
                apiVersion: v1
                kind: Secret
                metadata:
                  name: ldap-provider-secret
                  namespace: openshift-config
                type: Opaque
                stringData:
                  bindPassword: '{{ (fromConfigMap "default" "ldap-config" "bindPassword") }}'
            {{ end }}      
            {{ if and (eq (fromConfigMap "default" "ldap-config" "ldap") "enabled") (eq (fromConfigMap "default" "ldap-config" "secure") "true") }}      
            - complianceType: musthave
              objectDefinition:
                apiVersion: v1     
                kind: ConfigMap
                metadata:
                  name: ldap-ca
                  namespace: openshift-config
                data:
                  ca.crt: '{{ (fromConfigMap "default" "ldap-config" "ldap-ca") }}'
            {{ end }}
            - complianceType: musthave
              objectDefinition:
                apiVersion: config.openshift.io/v1
                kind: OAuth
                metadata:
                  name: cluster
                spec:
                  identityProviders:
                    - name: htpasswd-provider
                      mappingMethod: claim
                      type: HTPasswd
                      htpasswd:
                        fileData:
                          name: htpasswd-secret
                     {{ if eq (fromConfigMap "default" "ldap-config" "ldap") "enabled" }}  
                    - name: ldapidp
                      mappingMethod: claim
                      type: LDAP
                      ldap:
                        attributes:
                          id:
                            - dn
                          email:
                            - mail
                          name:
                            - cn
                          preferredUsername:
                            - uid
                        bindDN: '{{ (fromConfigMap "default" "ldap-config" "bindDN") }}'
                        {{ if ne (fromConfigMap "default" "ldap-config" "bindPassword") "" }}
                        bindPassword:
                          name: ldap-provider-secret
                        {{ end }}  
                        {{ if eq (fromConfigMap "default" "ldap-config" "secure") "true" }}
                        ca:
                          name: ldap-ca
                        {{ end }}
                        insecure: {{ if eq (fromConfigMap "default" "ldap-config" "secure") "true" }}false{{else}}true{{ end }}
                        url: '{{ (fromConfigMap "default" "ldap-config" "url") }}'
                     {{ end }}
            - complianceType: musthave
              objectDefinition:
                apiVersion: rbac.authorization.k8s.io/v1
                kind: ClusterRoleBinding
                metadata:
                  name: ocpadmin-cluster-admin
                subjects:
                  - kind: User
                    name: ocpadmin
                    apiGroup: rbac.authorization.k8s.io
                roleRef:
                  apiGroup: rbac.authorization.k8s.io
                  kind: ClusterRole
                  name: cluster-admin
