{{- .Values.disconnected }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: custom-ca-bundle
  namespace: openshift-config
data:
  {{- range .Values.registries }}
  {{ .host | replace ":" ".." | quote }}: |-
    {{- .cert | nindent 4 }}
  {{- end }}


{{-end }}
